<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Obituaries</title>
</head>
<body>
    <h1>Obituaries</h1>
    <ul>
        <% obituaries.forEach(obituary => { %>
            <li><a href="/obituary/<%= obituary.slug %>"><%= obituary.name %></a></li>
        <% }); %>
    </ul>
    <a href="/submit">Submit New Obituary</a>
</body>
</html>
